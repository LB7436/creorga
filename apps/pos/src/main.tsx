import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
// Les données ne sont synchronisées qu'après connexion à une société.

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
